Static UI assets exported from Figma are stored in this directory.

The `template-arrow-*`, `template-maximize-2`, and `template-minimize-2` SVG icons are from Lucide (ISC License): https://lucide.dev/
